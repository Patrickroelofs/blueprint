import { type Edge, type Node } from '@xyflow/react';

function computeFlow(nodes: Node[], edges: Edge[]): void {
  console.log(nodes, edges);
  // TODO: Implement computeFlow functionality, this should automatically compute the flow of the nodes and edges
}

export { computeFlow };
